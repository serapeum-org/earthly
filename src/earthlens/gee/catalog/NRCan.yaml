# Auto-grouped slice of the GEE catalog: NRCan.
# 1 dataset(s). Edit in place; the loader merges every
# *.yaml file in this directory into one Catalog at import time.

datasets:
  NRCan/CDEM:
    title: Canadian Digital Elevation Model
    provider: NRCan
    ee_type: image_collection
    spatial_resolution: 23.19
    extent:
      start_date: "1945-01-01"
      end_date: null
      bbox: [-142, 41, -52, 84]
    default_reducer: median
    license: unknown
    terms_note: 'Licensed under the'
    bands:
      elevation:
        description: Elevation
        units: m
        min: -226
        max: 5944

