# Auto-grouped slice of the GEE catalog: TOMS.
# 1 dataset(s). Edit in place; the loader merges every
# *.yaml file in this directory into one Catalog at import time.

datasets:
  TOMS/MERGED:
    title: TOMS and OMI Merged Ozone Data v3 [deprecated]
    provider: NASA / GES DISC
    ee_type: image_collection
    cadence: { interval: 1, unit: day }
    spatial_resolution: 111000
    extent:
      start_date: "1978-11-01"
      end_date: null
    default_reducer: median
    license: public-domain
    terms_note: 'Distribution of data from the Goddard Earth Sciences Data and'
    bands:
      ozone:
        description: Total column ozone
        units: Dobson
        min: 73
        max: 983

